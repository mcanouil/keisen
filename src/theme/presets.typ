///! Theme presets.
///!
///! A theme is an option dictionary, so presets compose with table-options()
///! rather than being a separate kind of thing. Borders, fills, and insets
///! arrive with milestone 5; this is the empty starting point.

#let theme-default() = (:)
